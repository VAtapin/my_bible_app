# PROJECT_STATUS

## Реализовано

- PWA на Vue 3/TypeScript остаётся самостоятельным браузерным клиентом для
  `https://my.bible-desktop.com`.
- В `mobile/` развивается отдельное Kotlin Multiplatform-приложение без WebView:
  Jetpack Compose для Android, SwiftUI для iOS, общий Ktor API-клиент и модели.
- Реализован фирменный RU/DE onboarding по согласованному UI: быстрый и ручной
  режимы, три шага, выбор разделов и независимый множественный выбор переводов.
- Выбор интерфейса, разделов и переводов сохраняется локально; после холодного
  запуска открывается собранное пользователем приложение.
- Реализовано нативное чтение Библии: выбранные переводы, каталог книг, главы,
  текст production API, изменение размера шрифта, переход по главам, закладки и
  системная отправка отдельного стиха.
- Android хранит полные карточки закладок, открывает их из раздела «Ещё» и
  автоматически восстанавливает последнее место чтения.
- Реализован нативный молитвослов: каталог по языку, чтение молитвы, размер
  текста и системная отправка.
- Реализован нативный церковный календарь: переход по дням, новый/старый стиль,
  пост, памяти и чтения дня.
- Те же маршруты и пользовательские сценарии подготовлены в SwiftUI; общий API
  предоставляет Swift-friendly loaders для переводов, Библии, молитв и календаря.
- Общие контракты production API покрыты unit-тестами для переводов, книг, главы,
  языковой фильтрации молитв и календаря.
- Bundle/application ID обеих платформ — `com.bibledesktop.myapp`.
- В изолированном каталоге `azbuka-web/` реализован первый рабочий Web App
  продукта «Азбука с цифирью»: церковнославянский интерфейс по умолчанию с
  переключением на RU/DE, главная, каталог из 37 букв и знаков, отдельный раздел
  цифири 1–999, упражнения с двумя вариантами, профиль и локальный прогресс в
  IndexedDB.
- Web App использует собственный app-like UI, локальный церковнославянский
  шрифт Ponomar (SIL OFL 1.1), устойчивые прямые маршруты, PWA manifest и
  service worker для запуска без обязательной установки и последующей работы
  без сети.
- Тренировка Web App работает без обязательного нажатия «Дальше»: верный ответ
  показывается около двух секунд и автоматически открывает следующее задание;
  после ошибки нужно нажать подсвеченный правильный вариант. Добавлены очки,
  серии, анимации обратной связи и виброотклик на поддерживаемых телефонах.
- На главной Web App добавлены живые цифровые часы: часы, минуты и секунды
  показываются церковнославянской буквенной цифирью, рядом остаётся обычная
  запись времени и пояснение об отсутствии позиционного нуля. Гражданская дата
  не показывается, пока не реализован проверенный богослужебный календарь.
- Каждая из 37 карточек получила проверенный контекстный пример из
  церковнославянского корпуса Bible Desktop с выделением изучаемой буквы,
  ссылкой на библейский источник и переводом RU/DE. Системная озвучка полностью
  отключена: она неверно читает церковнославянский Unicode; воспроизведение
  вернётся только с собственным проверенным аудиопакетом.
- Цифирь отделена от азбуки и общей буквенной тренировки. Новый самостоятельный
  экран показывает запись выбранного числа, пошаговое разложение, особое правило
  11–19, три основных правила и таблицы единиц, десятков и сотен.

## Текущее состояние

Android debug APK собирается, установлен на эмулятор API 37 и проверен на живом
production API. Полностью пройдены быстрый и ручной onboarding, холодный запуск,
чтение немецкой главы, сохранение закладки, русский молитвослов и календарь.

iOS-реализация поддерживает те же основные сценарии на уровне исходников, но её
нужно собрать и визуально проверить на macOS/Xcode. Store-релиз пока не готов:
не завершены подпись, store metadata, offline-кэш, уведомления и release QA.

Старые `android/` и `ios/` Capacitor-проекты сохранены только как исходная
реализация; целевой мобильный код находится в `mobile/`.

Первый вертикальный браузерный релиз «Азбуки» собирается самостоятельно из
`azbuka-web/`. Он проверен на ширинах 320 и 390 CSS px, проходит полный сценарий
от onboarding до результата тренировки и сохраняет прогресс после перезагрузки.
Production deployment на `azbuka.bible-desktop.com` ещё не выполнялся.

Цикл ответа в тренировке различает первый результат и учебное исправление:
ошибка сохраняется в статистику один раз, повторное нажатие на правильный ответ
не накручивает точность или очки. Очки и серия начисляются только за правильный
ответ с первой попытки.

Главная показывает локальное текущее время с секундами одновременно обычными
цифрами и церковнославянской цифирью. В карточках букв контекст встроен в
офлайн-пакет;
пример «Аз» использует Ин 14:6, редкие буквы получают отдельные корпусные
фрагменты (например, «Пси» — 2 Цар 23:1).

## Важные решения

- PWA, Android и iOS — три клиента одного Bible Desktop API.
- Для отдельного образовательного продукта «Церковнославянская азбука»
  подготовлено ТЗ в `docs/AZBUKA_PRODUCT_SPEC.md`. Продукт не является модулем
  текущего приложения: рекомендуемый application/bundle ID —
  `com.bibledesktop.azbuka`, исходный код и релизный цикл должны находиться в
  отдельном репозитории. Кроме нативных Android/iOS-клиентов обязательна Web App,
  доступная без установки; рабочий URL — `https://azbuka.bible-desktop.com`, а
  PWA-установка остаётся дополнительной возможностью.
- До выделения самостоятельного репозитория код нового Web App временно
  размещён в строго изолированном каталоге `azbuka-web/`; он не импортирует код
  и состояние существующего Bible Desktop.
- Android UI пишется на Jetpack Compose, iOS UI — на SwiftUI; KMP разделяет API,
  модели и тестируемую прикладную логику, но не UI.
- Основа UI — вариант №1 из `UI/`; из варианта №3 перенесены три шага, крупные
  зоны нажатия, переключатели, компактный выбор переводов и панель чтения.
- Язык интерфейса не ограничивает языки Библии. Приоритетным сначала становится
  перевод языка интерфейса, но пользователь может мгновенно выбрать любой из
  добавленных переводов.
- Приложение не показывает русский молитвенный текст как немецкий: ответы API
  дополнительно фильтруются по фактическому `language_code`.
- Идентификатор — `com.bibledesktop.myapp`; версия пока `0.1.0` (build 1).
- Android: min SDK 26, compile/target SDK 37, JDK 25. iOS: minimum 16.0.
- Секреты подписи Android/iOS не хранятся в Git.

## Известные проблемы

- Production endpoint `/api/prayers?language=de` сейчас возвращает 23 русских
  записи с `language_code=ru`; поэтому немецкий молитвослов корректно показывает
  пустое состояние. Немецкий корпус требуется импортировать в основной API.
- iOS build, Swift/Kotlin interop и визуальная проверка требуют macOS/Xcode.
- Общий offline-кэш для глав, молитв и календаря ещё не реализован.
- APNs/FCM, локальные расписания уведомлений, deep links и store-подпись ещё не
  подключены.
- Общий каталог закладок и восстановление последнего места пока не перенесены в
  SwiftUI; Android-реализация уже готова и проверена.
- Экран «Сегодня» использует локализованную статическую цитату, а не endpoint
  чтений дня.
- Учебные подписи, переводы и подбор фрагментов «Азбуки» требуют итоговой
  редакторской проверки специалистом. Записанный аудиопакет пока отсутствует;
  системный Speech Synthesis запрещён и кнопки воспроизведения нет.
- В Web App «Азбуки» ещё не реализованы надстрочные знаки, слова под титлами,
  очередь повторения ошибок, отдельные игровые режимы и синхронизация; это
  следующие этапы ТЗ, а не функции текущего вертикального релиза.
- Для production Web App не определён подтверждённый Plesk document root и не
  выполнен deploy на `azbuka.bible-desktop.com`.

## Рекомендуется следующим

1. Развернуть `azbuka-web/dist` на staging `azbuka.bible-desktop.com`, проверить
   HTTPS, прямые маршруты, service worker и offline restart в production-браузере.
2. Провести редакторскую проверку 37 карточек, церковнославянской локализации и
   правил цифири; затем подготовить собственные записи полного текста каждого
   примера с реестром источников и лицензий.
3. Реализовать второй этап Web App: надстрочные знаки, титла, отдельные игровые
   упражнения цифири и очередь повторения ошибок; затем перенести общий учебный
   движок в KMP для нативных Android/iOS-клиентов `com.bibledesktop.azbuka`.
4. Перенести Android-каталог закладок и восстановление места чтения основного
   Bible Desktop в SwiftUI.
5. Реализовать offline-кэш выбранных переводов/глав и сохранение молитв/календаря.
6. Добавить нативные локальные уведомления и deep links.
7. Импортировать немецкие молитвенные тексты в основной Bible Desktop API.
8. На Mac собрать `mobile/iosApp/iosApp.xcodeproj`, исправить возможные Swift/KMP
   interop-замечания и выполнить визуальный QA.
9. После release QA подготовить подпись и store-артефакты Android/iOS.

## Проверки

- ТЗ «Церковнославянская азбука» проверено на полноту структуры, согласованность
  идентификатора, поддержку Android/iOS/Web и отсутствие требований копировать
  материалы референса.
- `:sharedLogic:testAndroidHostTest` — пройдено.
- `:androidApp:assembleDebug` — BUILD SUCCESSFUL.
- Android API 37: ручной и быстрый onboarding, RU/DE, сохранение профиля после
  холодного запуска — проверено.
- Android API 37: список книг, смена RST/Elberfelder, выбор главы, немецкий текст,
  изменение размера, закладка `Gen.1.1` — проверено на production API.
- Android API 37: русский каталог и чтение молитвы, календарь текущего дня —
  проверено на production API.
- Android API 37: полная карточка закладки, каталог «Ещё», открытие сохранённой
  главы и восстановление последнего места — проверено.
- iOS build не запускался: Windows не поддерживает Xcode.
- `azbuka-web`: `npm run check` — пройдены TypeScript, 21 unit-тест и
  production Vite/PWA build.
- `azbuka-web`: HTTP smoke test production preview — `/`, прямой маршрут
  `/alphabet/az` и `/sw.js` возвращают HTTP 200; `.htaccess` присутствует в
  `dist`.
- `azbuka-web`: браузерный smoke test — RU/DE onboarding, карточка буквы,
  сохранение изученного элемента, полная тренировка, итог, статистика и
  сохранение IndexedDB после reload проверены.
- `azbuka-web`: responsive QA на 320 и 390 CSS px — горизонтального
  переполнения нет, колонки равномерны, Ponomar загружается локально.
- `azbuka-web`: в браузере проверены неверный ответ, подсветка правильного,
  исправление, автоматический переход через 1,8 секунды, очки и серия; мобильный
  экран ошибки проверен на ширине 390 CSS px.
- `azbuka-web`: в браузере проверены церковнославянский первый запуск, часы с
  живыми секундами, отдельный разбор числа 742, отсутствие числового режима в
  азбуке и отсутствие кнопки системной озвучки. Переключение CU/DE проверено;
  экран цифири на мобильной ширине 390 CSS px отображается без переполнения,
  ошибок в консоли нет.

## Последний связанный commit

- `9b71302a83092b1ec4046b3ff9b00a90619b5ece` — нативный onboarding и рабочие
  сценарии Библии, молитв и календаря Android/iOS.
- `e8bc1ea2103109bb8a135db2ce8618f8d0727956` — каталог закладок и восстановление
  чтения Android.
- `06d3e6e2600ce69ee07bd2c82736218bb5c1ee2b` — исправление обратной навигации
  onboarding и настроек.
- `7578c25afbebc3f5a77ee371d9c5438a50c5db09` — техническое задание отдельного
  приложения «Церковнославянская азбука».
- `2030a63db045e6de68a5c6dca56101f4f83ae19f` — обязательная браузерная Web App
  добавлена в ТЗ «Церковнославянская азбука».
- `88c37331c87944cb5b190c5d8141f8157b30cb32` — первый рабочий вертикальный
  релиз `azbuka-web`.
- `31203a1bc47448373ff1c01e9d399e86cbde18e9` — автоматический игровой цикл
  ответа в тренировке Web App.
- `12ff1121bfc1f67d62cabe4f057e93183c6a05c1` — церковнославянские часы и
  контекстные примеры всех букв.
- Текущий commit: церковнославянский интерфейс по умолчанию, секунды и отдельный
  учебный инструмент цифири.
