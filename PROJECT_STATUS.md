# PROJECT_STATUS

## Реализовано

- Создан исполняемый каркас Vue 3 + TypeScript + Vite + Capacitor для PWA,
  Android и iOS в одной кодовой базе.
- Подключены маршрутизация, Pinia, PWA service worker и локальные уведомления
  Capacitor.
- Реализован типизированный клиент публичного Bible Desktop API с классификацией
  ошибок и проверкой критических ответов.
- Реализован первый вертикальный сценарий: загрузка одной главы, сохранение в
  IndexedDB и повторное открытие локальной копии.
- Созданы и синхронизированы нативные проекты `android/` и `ios/`.
- Подтверждены production API, CORS и рабочая глава
  `BQ_RUSSIAN_RST_STRONG / genesis / 1`.
- Подготовлены Plesk deployment-скрипт и SPA fallback для публикации PWA на
  `https://my.bible-desktop.com`.
- Документированы отдельные безопасные команды первого Git-развёртывания и
  последующих обновлений уже развёрнутого приложения.
- Интерфейс прототипа приведён к фирменному стилю Bible Desktop: используется
  утверждённая сине-кремовая палитра, мобильная app-shell, нижняя навигация и
  иконки из `D:\Projekte\BibleDesktop\UI`.
- Обновлены PWA-, Android- и iOS-иконки, splash-экраны и отображаемое имя
  приложения. Правила оформления закреплены в `docs/DESIGN_SYSTEM.md`.
- Реализован этап 1: первый запуск, быстрый и ручной конструктор, сводка,
  версионированная локальная конфигурация, экран «Сегодня» и повторное действие
  «Настроить моё приложение».
- Рабочее чтение главы сохранено как отдельный раздел общей мобильной навигации.

## Текущее состояние

Клиентская часть этапа 1 реализована локально и покрыта модульными тестами.
Опубликованная PWA на `https://my.bible-desktop.com` требует обновления до
текущей версии. Нативные оболочки созданы и синхронизированы, но SQLite,
физические устройства и нативные сборки ещё не проверены.

## Важные решения

- PWA, Android и iOS развиваются в одной ветке продукта и общей кодовой базе;
  платформенные различия реализуются адаптерами.
- IndexedDB используется для PWA; мобильное хранилище будет подключено за
  интерфейсом `ChapterRepository` после device-проверки SQLite-библиотеки.
- Контент и бизнес-логика остаются в Bible Desktop; новый проект является
  клиентом его публичного API.
- Production-каталог проекта — `/var/www/vhosts/bible-desktop.com/my_app`,
  document root субдомена — `/var/www/vhosts/bible-desktop.com/my_app/dist`.
- Зафиксирован Capacitor 8.4.3: эта согласованная версия не содержит найденную
  npm audit-уязвимость ветки CLI 8.5.x.
- Источник визуальной системы — `D:\Projekte\BibleDesktop\UI`; основные цвета:
  `#4A6B8A`, `#7DA1C2`, `#EDE6D6`, `#F7F5F1` и акцентный `#B99A5A`.
- Золотой используется только как небольшой декоративный акцент; основные
  поверхности и действия оформляются фирменным синим.
- Небольшая несекретная конфигурация хранится в localStorage под версионированным
  ключом `bible-desktop:profile:v1` и проверяется при чтении. Контент остаётся в
  IndexedDB/SQLite, а серверные секреты восстановления локально не хранятся.

## Известные проблемы и открытые вопросы

- На рабочей машине отсутствуют JDK и Android SDK, поэтому Gradle-сборка не
  запускалась; iOS-сборка требует macOS/Xcode.
- Не выбрана и не проверена SQLite-библиотека на физических устройствах.
- Локальное уведомление реализовано, но ещё не проверено на Android и iOS.
- В основном API пока нет профиля, восстановления, offline-манифестов,
  синхронизации по ревизиям и регистрации push-токенов.
- Молитвы, календарь и изучение уже выбираются в конструкторе, но их контентные
  экраны ещё не реализованы и явно помечены как следующие этапы.

## Рекомендуется следующим

Развернуть конструктор на production и проверить полный первый запуск на
телефоне. Следующий программный блок — этап 2: API-списки переводов и книг,
последнее место чтения, закладки и управление офлайн-данными. Параллельно нужен
физический Android-тест SQLite и уведомления.

## Проверки

- `npm audit` — 0 уязвимостей.
- `npm run check` — typecheck, 10 модульных тестов и production PWA build прошли.
- `npx cap sync` — Android и iOS синхронизированы.
- Локальная визуальная проверка — интерфейс, загрузка главы из production API и
  открытие сохранённой копии в IndexedDB работают.
- В браузере пройден быстрый первый запуск, создание локального профиля, экран
  «Сегодня», повторное редактирование и переход к чтению.
- Нативные иконки Android/iOS и светлый splash-экран проверены визуально.
- Живой production API-запрос — переводы, книги, глава и CORS подтверждены.
- Production build содержит `.htaccess`, `index.html`, manifest и service worker.
- Production smoke test — HTTPS, HTML, JS, CSS, manifest, service worker и SPA
  fallback отвечают корректно на `my.bible-desktop.com`.
- Android Gradle build не запускался: в окружении нет JDK и Android SDK.
- iOS build не запускался: текущая среда Windows не поддерживает Xcode.

## Последний связанный commit

- Конструктор и локальный профиль Bible Desktop (этот commit).
